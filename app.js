document.querySelectorAll(".edit-team").forEach(b=>b.addEventListener("click",()=>{const code=prompt("3FS Team access code:");if(code!=="3FS-TEAM") return alert("Team access only.");const n=prompt("Edit team member name:",b.dataset.name);if(n){b.dataset.name=n;b.closest(".member").querySelector("h3").textContent=n;alert("Saved on this device. Real production authentication/database should be connected before launch.")}}));document.getElementById("enquiryForm")?.addEventListener("submit",e=>{e.preventDefault();alert("Enquiry saved for this trial website. Connect the secure live database before public launch.");});